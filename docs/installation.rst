============
Installation
============

At the command line::

    $ easy_install healthy_django

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv healthy_django
    $ pip install healthy_django
